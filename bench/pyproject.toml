[project]
name = "conductor-fabric-bench"
version = "0.1.0"
description = "Benchmark runner for Conductor Fabric"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
